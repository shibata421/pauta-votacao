spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console.enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

cpf:
  validationUrl: https://user-info.herokuapp.com/users/{cpf}
